# `charted-server` uses a nightly version of Rust to have the following features
# that haven't landed in stable:
#
# - `once_cell_try` ()
# - `ptr_as_ref_unchecked` ()
# - `decl_macro` ()
# - `never_type` ()
#
# Not sure if we can use a nightly version of Rust here?
{}
